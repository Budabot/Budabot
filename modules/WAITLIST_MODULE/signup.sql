CREATE TABLE IF NOT EXISTS signup_lists (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(50) NOT NULL, owner VARCHAR(20) NOT NULL, dt INT NOT NULL);
CREATE TABLE IF NOT EXISTS signup_characters (list_id INT NOT NULL, name VARCHAR(20) NOT NULL);