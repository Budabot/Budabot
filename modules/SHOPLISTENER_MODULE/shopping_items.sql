CREATE TABLE IF NOT EXISTS shopping_items (id INT PRIMARY KEY AUTO_INCREMENT, message_id INT NOT NULL, lowid INT NOT NULL, highid INT NOT NULL, ql SMALLINT NOT NULL, iconid INT NOT NULL, name VARCHAR(150) NOT NULL);