CREATE TABLE IF NOT EXISTS shopping_messages (id INT PRIMARY KEY AUTO_INCREMENT, dimension SMALLINT NOT NULL, message_type SMALLINT NOT NULL, channel VARCHAR(50) NOT NULL, bot VARCHAR(20) NOT NULL, sender VARCHAR(20) NOT NULL, dt INT NOT NULL, message VARCHAR(2000));