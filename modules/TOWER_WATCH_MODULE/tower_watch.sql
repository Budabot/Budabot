CREATE TABLE IF NOT EXISTS tower_watch ( id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, zone CHAR(12) NOT NULL, base_number INTEGER NOT NULL, close_time INTEGER NOT NULL, ct_ql INTEGER NOT NULL, side CHAR(7) NOT NULL, org CHAR(50) NOT NULL );
